<form method="post" action="{{ path('admin.property.option.delete', {'id': property_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property_option.id) }}">
    <button class="btn">Delete</button>
</form>
