{% extends 'base.html.twig' %}

{% block title %}PropertyOption{% endblock %}

{% block body %}
    <h1>PropertyOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin.property.option.index') }}">back to list</a>

    <a href="{{ path('admin.property.option.edit', {'id': property_option.id}) }}">edit</a>

    {{ include('admin/option/_delete_form.html.twig') }}
{% endblock %}
